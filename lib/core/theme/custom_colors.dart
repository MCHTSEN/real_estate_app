import 'package:flutter/material.dart';

class CustomColors {
  static const Color primary = Color.fromARGB(255, 25, 101, 2);
  static const Color secondary = Color(0xFF000000);
  static const Color accent = Color(0xFF000000);
}